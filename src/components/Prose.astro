<div class="prose"><slot /></div>

<style is:global>
  /* Long-form rendered Markdown. Emitted globally (not scoped) because Astro
     scoped styles cannot reach content rendered by <Content />. The layer
     order is re-declared here so `components` stays above `base` regardless of
     stylesheet bundle order. */
  @layer reset, base, components, utilities;

  @layer components {
    .prose {
      max-width: var(--measure);
      color: var(--c-ink);
    }
    .prose > * + * {
      margin-top: 1.45em;
    }
    .prose h2 {
      font-size: var(--text-h2);
      margin-top: 2.2em;
      line-height: 1.15;
    }
    .prose h3 {
      font-size: var(--text-h3);
      font-weight: 480;
      margin-top: 1.8em;
    }
    .prose p,
    .prose li {
      line-height: 1.75;
    }
    .prose a {
      color: var(--c-accent);
      text-decoration: underline;
      text-decoration-thickness: 1px;
      text-underline-offset: 0.18em;
      text-decoration-color: color-mix(
        in oklab,
        var(--c-accent) 38%,
        transparent
      );
      transition:
        text-decoration-color var(--dur) var(--ease-out),
        color var(--dur-fast) var(--ease-out);
    }
    .prose a:hover {
      color: var(--c-accent-deep);
      text-decoration-color: currentColor;
    }
    .prose ul,
    .prose ol {
      padding-left: 1.3em;
    }
    .prose li + li {
      margin-top: 0.45em;
    }
    /* Indented editorial pull-quote: no colored side-stripe (banned). */
    .prose blockquote {
      font-family: var(--font-display);
      font-style: italic;
      font-weight: 340;
      font-size: var(--text-lede);
      line-height: 1.42;
      color: var(--c-ink-soft);
      padding-inline-start: 1.4rem;
      margin-block: 1.8em;
    }
    .prose blockquote p {
      line-height: 1.42;
    }
    .prose code:not(pre code) {
      background: var(--c-surface);
      border: 1px solid var(--c-hair);
      border-radius: var(--radius-sm);
      padding: 0.1em 0.35em;
    }
    .prose pre {
      background: var(--c-surface);
      border: 1px solid var(--c-hair);
      border-radius: var(--radius-md);
      padding: 1rem 1.15rem;
      overflow-x: auto;
      line-height: 1.6;
    }
    .prose pre.astro-code {
      background: var(--c-surface) !important;
    }
    .prose img {
      border-radius: var(--radius-md);
    }
    .prose hr {
      margin-block: 2.5em;
    }
  }
</style>
