---
// The long-form Markdown styles live in a global stylesheet (not a CSS module)
// because CSS Modules hash class names, but <Content /> renders bare
// <h2>/<p>/<table> elements with no class hooks to target. See prose.css.
import '../styles/prose.css';
---

<div class="prose"><slot /></div>
